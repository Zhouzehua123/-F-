# 第三题修改说明

本次以 `论文.tex` 的实际引用链为事实源，在 `syq` 分支修改第三题；保留广义标度律、三类成本函数、SLSQP 求解框架和已有结果 CSV。未更换模型，未重新运行优化器。第一、二题正文和求解代码未修改。

## 文字与一致性

- 参考第二题的结构与语气重写第三题，保留“具体分析—模型准备—模型建立—模型求解”。
- 明确绝对参数数/token 数与十亿单位的换算；局部以 `C_q`、`C_b` 区分质量系数和预算；将 `tau(Q)` 解释为等效附加参数规模。
- 明确基准表的配比项为 `M=0`；推荐配比属于固定损失平移情景，并非第四个共同寻优变量。
- 修正质量边际收益递减、达到质量上界后无需支付质量成本等解释；不改变原质量函数。
- 将原“完成度弹性”改为代码实际计算的 `dTheta/dlog10(C)`；将预算门槛改为网格识别值。首次正投入和首次饱和仍为原 CSV 支持的 1.78e19、7.08e19 FLOPs。
- 修正中预算数据量差异不超过 10% 的表述，改为 229.5–274.2B tokens（最大值较最小值高约 19.5%）。
- 同步修改公共符号表、摘要中仅第三题的一段、模型检验中对应成本分界与配置阶段的一段。摘要原将 32768 tokens 的配置与 131072 tokens 的损失增幅混写，现已统一。
- 沿用第二题对质量尺度衔接、配比效应迁移和外推范围的限制。

## 图表

`求解/问题三/论文绘图.py` 独立读取结果 CSV，输出同名 300 dpi PNG 和矢量 PDF；正文改为引用矢量 PDF。

1. 三档预算图使用真实对数预算间距，统一单位和标注。
2. 结构性转移图增加转移区间放大，展示质量完成度与三类预算份额，并标明网格边界和份额峰值。
3. 上下文敏感性图直接使用 token 长度为横轴，标出 30000 tokens 成本分界与五个附件档位，注明预算和重合曲线。
4. 成本函数图统一基线 `Q0=0.5510`、增量成本单位和图例。

缺失点不做插值：预算扫描 141 点中有 140 个有效点；连续上下文扫描 40 点中有 37 个有效点；三个质量响应情景分别有 11/14、13/14、12/14 个有效点。绘图脚本按原扫描网格恢复缺口，避免误画成完整成功求解的曲线。

短表改为普通浮动三线表，避免九行成本对比表被自动拆成续表。

## 文献依据

本次使用已有参考文献键，没有新增或更改书目条目。

- Hoffmann et al., *Training Compute-Optimal Large Language Models*：https://arxiv.org/abs/2203.15556 。核对摘要及原文附录 F（PDF 第 28 页），确认固定预算下参数与 token 数协同配置，以及常用 `C=6DN` 算力近似。本文拟合的增长指数不声称与该文指数一致。
- Muennighoff et al., *Scaling Data-Constrained Language Models*：https://arxiv.org/abs/2305.16264 。核对原始摘要，引用其关于重复数据收益下降与数据供给约束的结论，不引入新的供给模型或实验结果。
- Xie et al., *DoReMi: Optimizing Data Mixtures Speeds Up Language Model Pretraining*：https://arxiv.org/abs/2305.10429 。核对原始摘要，引用其配比影响预训练效率的结论。它不验证本文固定平移假设或“约 30 倍”数值。
- `data2026f`：沿用论文已有赛题材料引用，标明质量与注意力成本的题设来源。当前仓库未包含题目原文与完整附件，因此没有声称已独立核验题设全文。

## 留待讨论的实质问题

1. 第一题熵权评分与附件 B 质量评分是否需要另建校准关系。本次仅明确已有衔接假设，不重新标定。
2. `gamma<1` 导致上界导数不有界；多初值 SLSQP 并不证明全局最优。若需要补强，可另行讨论独立降维搜索、边界比较及失败点重算，不据此直接替换模型。
3. 原 `问题三.py` 与 `结构性转移识别.csv` 中仍存在旧字段名称：“弹性”实际为完成度变化率，“启动边界”实际以份额变化率峰值计算。本次正文和独立绘图按真实定义解释，并从预算 CSV 直接识别首次正投入，没有修改历史计算文件。
4. 高预算配置及约 30 倍算力等价属于模型外推情景，尚无新增实验验证。

## 编译说明

本机初始没有可用的 XeLaTeX。采用临时目录中的 Tectonic（XeTeX 引擎）编译完整论文，宏包缓存由编译器下载。为解决本机字体名称识别问题，仅在临时编译副本中把模板指定的 DejaVu Sans Mono 改成同名字体文件的显式路径；仓库 `format.cls` 未改动。其余源文件与最终工作区一致。

全文仍含其他章节既有的排版警告，代码附录中部分 emoji 在等宽字体中缺字；本次不改动其他题目的代码与排版。最终核查以第三题正文、四张图、相关短表和引用为重点。

## 第二版：重组论证与精简图表

- 将建模顺序改为单位 token 的成本、预算约束、参数—数据平衡、数值求解；公式仅作等价整理，新增的饱和质量份额表达式由原预算等式直接得到。
- 将结果改为预算变化、成本形式对照、上下文影响、配比与适用范围；合并重复限制，减少逐项复述表格和模板式过渡。
- 三档配置图从正文移除，保留源图片作为备用；删除正文逐点阈值表，改用关键边界区间叙述。
- 转移图由四面板改为双面板加内嵌局部放大，维持相同 CSV、门槛和缺失点；其余图保留。
- 三类成本函数改为紧凑表格；通过主文件加载 placeins，仅在第三题设置局部浮动边界，防止图表跨入下一结果小节。
- 现有原始结果和求解器不变。第二版完成后先保留为本地审阅稿，未自动更新远程 PR。
