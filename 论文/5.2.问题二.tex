\Needspace{8\baselineskip}
\subsection{问题二：跨维度数据融合与广义标度律}
\begingroup
\setlength{\tabcolsep}{2pt}
\clubpenalty=10000
\widowpenalty=10000

\subsubsection{具体分析}

参数和数据量相同，并不保证训练损失相同，语料质量及其领域构成也可能起作用。本问要把这些影响纳入规模关系，进而回答：改善数据能够代替多少参数投入？困难在于，附件 A 主要记录配比，附件 B 主要记录规模与质量，没有一套资料同时包含全部变量。因此，建模前先明确哪些评分可以比较、哪些配比结果需要附加迁移条件。

图\ref{fig:p2_framework}给出了本问的计算顺序。整理数据用途、检查质量方向后，先用 Pythia 拟合规模关系，并留出大模型检验。再比较四种质量项，选择合适形式，利用弹性与等损失关系分析投入收益。最后把问题一的配比系数接入模型，通过不同来源的对照说明结果的使用范围。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图0_问题二分析框架.png}
  \caption{规模、质量与领域效应的分析框架}
  \label{fig:p2_framework}
\end{figure}

\subsubsection{模型准备}

\textbf{1.数据组织与变量口径}\par\nopagebreak[4]
规模关系由 B1 估计。该附件覆盖 8 个 Pythia 模型，每个模型记录 147 个检查点，总计 1{,}176 条\cite{biderman2023pythia}。质量关系主要从 B6 半合成实验取得，B7 只保留与 B6 不重复的部分作扩展检查。剩余附件按用途分别对照：训练轨迹看 B2、B3，跨族及文献结果看 B4、B5，质量方向看 B8，大模型的估算损失则由 B9、B10 匹配后比较。

验证集交叉熵损失记为 $L$，三个解释量为参数量 $N$、数据量 $D$ 和质量 $Q$。为便于表示规模，本节以十亿参数和十亿 tokens 分别计量 $N$、$D$；问题三核算算力时再各乘 $10^9$。拟合保留原函数中的常数项 $E$，直接求解，避免取双对数时把不可约损失漏掉。

\textbf{2.质量字段方向检验}\par\nopagebreak[4]
质量高分是否真的对应较低损失，要先核对字段方向。为减少规模差异的影响，在每个固定 $(N,D)$ 的实验组内计算 $Q_{\text{score}}$ 与损失的 Pearson 相关，再求组间算术平均。均值为负时，方向与本文的质量定义相符；均值为正时，则需要进一步检查。表\ref{tab:p2_dir}据此说明方向，不据此作显著性判断。

\begin{table}[htbp]
  \centering
  \caption{质量评分方向与样本使用方式}
  \label{tab:p2_dir}
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{5pt}
  \begin{tabularx}{0.98\textwidth}{@{}
    >{\hsize=0.65\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=0.65\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=0.70\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=2.00\hsize\linewidth=\hsize\raggedright\arraybackslash}X@{}}
    \toprule
    数据分组 & \makecell{平均组内\\相关系数} & \makecell{记录数 /\\实验组数} & 评分解释与使用方式 \\
    \midrule
    B6 & $-0.925$ & 360 / 45 & 沿用原评分；$Q=1$ 为最优参考 \\
    B7 & $-0.918$ & 450 / 45 & 与 B6 同向，仅新增记录用于检验 \\
    B8 校准段 & $+0.995$ & 984 / 90 & 方向相反，翻转评分后作诊断 \\
    B8 外推段 & $+0.968$ & 720 / 60 & 方向相反，不纳入主拟合 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.98\textwidth}\normalsize
  注：实验组固定 $N,D$。B7 包含 B6 的全部 360 条记录，新增 90 条；均值只描述方向，不表示显著性。
  \end{minipage}
\end{table}

B6、B7 的评分与损失呈负相关，组内相关的平均值分别为 $-0.925$、$-0.918$，因而可沿用原方向，令 $Q=Q_{\text{score}}$。但它们不是两份独立证据：逐行核对后，B7 包含 B6 全部 360 条记录，真正新增的只有 90 条。B8 的两个子集则与 B6/B7 方向相反，还有部分损失低于模型下限，故不参与主拟合，仅把评分翻为 $1-Q_{\text{score}}$ 后检查偏差。

图\ref{fig:p2_direction}列出各实验组的相关系数。每个圆点为一组固定 $(N,D)$ 的相关，菱形为组间平均；B6/B7 的点多数为负，B8 多数为正，方向差异并非个别组造成。另外，B6 与 B7 点群接近部分来自记录重复，不能把相似图形再算作一次独立验证。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图7_质量方向对照.png}
  \caption{质量字段与损失的组内相关分布}
  \label{fig:p2_direction}
\end{figure}

\begin{samepage}
方向一致之后，还要区分分数的尺度。问题一的综合评分与附件 B 的 $Q_{\text{score}}$ 计算方法不同，同为某个分值未必代表同等质量。因此，本节估计质量参数、解释质量增量时，统一使用附件 B 的评分尺度。
\par
\end{samepage}

\subsubsection{模型建立}

\textbf{1.经典标度律}\par\nopagebreak[4]
先在参考质量下考察规模效应。采用 Chinchilla 标度律\cite{hoffmann2022chinchilla}，把验证损失 $L$ 写成参数量与数据量的函数：
\begin{equation}
    L(N,D) = E + \frac{A}{N^{\alpha}} + \frac{B}{D^{\beta}}
\end{equation}
当 $N,D\to\infty$ 时，两项规模损失逐渐消失，剩下的就是 $E$。参数或数据增加得越多，相应幂函数项越小；下降幅度由 $A$、$B$ 决定，快慢由 $\alpha$、$\beta$ 决定。这样，在固定参考质量的前提下，便能分别计算扩参数和加数据的收益。

\textbf{2.质量项的选择与损失修正}\par\nopagebreak[4]
质量可以通过两种方式进入模型：一是改变有效参数或有效数据量，二是单列质量不足带来的损失。我们据此比较四个候选形式，使用同一组联合样本，并查看 B6 拟合、B7 新增点预测相关及残差信息评分，结果列于表\ref{tab:p2_form}。经典模型与候选模型均保留常数和幂函数项，用带界约束的非线性最小二乘估计，同时采用 soft-$L_1$ 损失减轻大残差的影响。

\begin{table}[htbp]
  \centering
  \caption{不同质量作用形式的拟合与扩展检验指标}
  \label{tab:p2_form}
  \renewcommand{\arraystretch}{1.15}
  \begin{tabularx}{0.98\textwidth}{@{}>{\raggedright\arraybackslash}X*{4}{>{\centering\arraybackslash}p{0.17\textwidth}}@{}}
    \toprule
    比较指标 & \makecell{加性质量缺口\\$C(1-Q)^\gamma$} & \makecell{质量同时乘\\$N$ 与 $D$} & \makecell{加性质量亏损\\$CQ^{-\gamma}$} & \makecell{质量作为\\有效数据乘子} \\
    \midrule
    联合样本 $R^2$ & \textbf{0.9896} & 0.9882 & 0.9848 & 0.9745 \\
    B6 样本 $R^2$ & \textbf{0.9556} & 0.9495 & 0.9349 & 0.8905 \\
    B7 新增点 $r$ & 0.991 & \textbf{0.991} & 0.991 & 0.983 \\
    待估参数个数 & 7 & 7 & 7 & 6 \\
    残差信息评分 $I_{\mathrm{RSS}}$ & $\mathbf{-10236}$ & $-10042$ & $-9660$ & $-8866$ \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.98\textwidth}\normalsize
  注：相关系数保留三位小数；加粗的最优指标按未舍入值确定。$I_{\mathrm{RSS}}$ 越小越优，仅用于同样本比较。
  \end{minipage}
\end{table}

\begin{samepage}
表\ref{tab:p2_form}中，四种质量项形式对 B7 新增点的相关都达到 $0.983$ 以上，单看相关很难取舍。进一步比较联合样本与 B6 的 $R^2$，加性质量缺口形式均为最高，故采用这一形式。相关系数之外，再用 RMSE、MAPE 衡量预测值的偏差。
\par
\end{samepage}

\begin{samepage}
辅助比较量记为 $I_{\mathrm{RSS}}=n\ln(\mathrm{RSS}/n)+2k$，$n$ 表示样本数，$k$ 表示参数数。估计采用 soft-$L_1$，并非高斯极大似然，因此不将该值严格解释为 AIC，只在相同样本之间比较。此外，B7 新增点参与过模型形式选择，其扩展检查仍属于开发阶段验证，不是完全独立的最终测试。
\par
\end{samepage}

将选定的质量缺口项加入规模基线后，损失关系写为：
\begin{equation}
    L(N,D,Q) = E + \frac{A}{N^{\alpha}} + \frac{B}{D^{\beta}} + C\,(1-Q)^{\gamma},
    \qquad C>0,\ \gamma>0
\end{equation}
\begin{samepage}
质量的参考点设在 $Q=1$。在此处质量项为零，函数便回到经典标度律形式，但联合拟合重新估计各项参数，并不要求数值也回到经典拟合结果。Pythia 没有实测质量，给它取 $Q=1$ 只是一项参考约定。若固定 $Q$，再让参数和数据量同时增大，则有
\par\nopagebreak[4]
\begin{equation}
    \lim_{N,D\to\infty} L(N,D,Q) = E + C\,(1-Q)^{\gamma} \;\ge\; E
\end{equation}
\end{samepage}
只要固定的质量仍低于 1，上式就会保留质量缺口。扩参数、加数据能够压低各自的规模项，却无法在这个加性模型中完全抵消质量不足。该结论来自所选函数的结构，实际使用时仍须限制在有数据支持的范围内。

\textbf{3.边际效用与弹性}\par\nopagebreak[4]
为刻画各因素微小变化对损失的影响，对广义模型求偏导，并在参考点 $(N_0,D_0,Q_0)$ 处评价：
\begin{equation}
    \frac{\partial L}{\partial N} = -\alpha A N^{-\alpha-1},\quad
    \frac{\partial L}{\partial D} = -\beta B D^{-\beta-1},\quad
    \frac{\partial L}{\partial Q} = -\gamma C (1-Q)^{\gamma-1}
\end{equation}
各偏导的单位不同。令参考点的预测损失为 $L_0$，按变量与损失的比例变化定义弹性：
\begin{equation}
    \varepsilon_N = \frac{\partial L}{\partial N}\frac{N_0}{L_0},\qquad
    \varepsilon_D = \frac{\partial L}{\partial D}\frac{D_0}{L_0},\qquad
    \varepsilon_Q = \frac{\partial L}{\partial Q}\frac{Q_0}{L_0}
\end{equation}

\textbf{4.质量提升与参数扩张的等效条件}\par\nopagebreak[4]
两种投入是否等效，可以从同一个起点比较。保持 $D$ 与配比不变，分别小幅增加质量或参数；若损失降幅相同，就以所需正向增量之比 $R_{NQ}$ 描述这两种方案：
\begin{equation}
    \begin{aligned}
      \left|\frac{\partial L}{\partial Q}\right|\mathrm dQ
      &=\left|\frac{\partial L}{\partial N}\right|\mathrm dN_{\mathrm{eq}},\\
      R_{NQ}=\frac{\mathrm dN_{\mathrm{eq}}}{\mathrm dQ}
      &=\frac{\gamma C(1-Q)^{\gamma-1}}{\alpha A N^{-\alpha-1}}
       =\frac{\gamma C}{\alpha A}(1-Q)^{\gamma-1}N^{\alpha+1}.
    \end{aligned}
\end{equation}
这里比较的是两种改善方案的投入增量。如果沿着同一损失水平移动，则应取 $\mathrm dN/\mathrm dQ=-R_{NQ}$，含义是质量提高后可减少参数。在 $Q$ 不变时，$R_{NQ}$ 按 $N^{\alpha+1}$ 增长，这是因为大模型再扩容的边际收益较小。上述比较只要求损失效果相等，是否节省算力，还要计算语料处理和训练各自的费用。

当质量增量 $\Delta Q$ 较大、一阶近似不再合适时，需要直接计算完整损失差。记质量改善带来的降幅为 $\delta_Q$，令参数扩张取得相同降幅，解得
\begin{equation}
  \begin{aligned}
    \delta_Q &= C\bigl[(1-Q)^\gamma-(1-Q-\Delta Q)^\gamma\bigr],\\
    \Delta N_{\mathrm{eq}}
      &= N\left[\left(1-\frac{\delta_Q N^\alpha}{A}\right)^{-1/\alpha}-1\right].
  \end{aligned}
  \label{eq:p2_finite}
\end{equation}
有限等效解有两个条件。质量增量先要满足 $0<\Delta Q\leq1-Q$，保证不越过上界；损失改善还要满足 $\delta_Q<AN^{-\alpha}$，因为参数扩张最多只能消除当前的参数项 $AN^{-\alpha}$。若两者相等，需要无限参数才能等效；若改善超过这一项，单靠扩容便无解。随着 $\Delta Q\to0$，完整关系退回 $\Delta N_{\mathrm{eq}}\approx R_{NQ}\Delta Q$。即使公式有解，超出拟合规模的数值也仍属外推。


\textbf{5.领域效应的相似性}\par\nopagebreak[4]
同一训练领域在不同验证任务上的作用也可能不同。问题一给出了领域 $i$、$j$ 在 13 个任务上的系数向量 $\bm a_i,\bm a_j$。先减去平均向量，再用余弦相似度比较它们的相对作用：
\begin{equation}
    s_{ij} = \frac{\langle \bm a_i-\bar{\bm a},\ \bm a_j-\bar{\bm a}\rangle}{\|\bm a_i-\bar{\bm a}\|\,\|\bm a_j-\bar{\bm a}\|}
\end{equation}
用 17 个领域系数向量的均值 $\bar{\bm a}$ 作中心后，$s_{ij}$ 接近 1 表示两域对不同任务的相对作用接近，接近 $-1$ 表示方向相反，可能存在任务取舍。但余弦值只保留方向，没有保留幅度，原回归也没有交互项。因此，它能帮助寻找可比较的领域，却不足以证明等量替换无损或混合后有额外收益。

\subsubsection{模型求解}

\noindent\textbf{辅助分析说明：} 本问数据整理、绘图与数值核对使用 Codex 辅助，模型为 GPT-5.4（OpenAI，2026-03-05）及 GPT-6 系列（OpenAI，2026-09-03 系列首次发布）\cite{ai_codex54,ai_codex6}。

\begin{samepage}
\textbf{1.经典标度律拟合与留出验证}\par\nopagebreak[4]
首先用 B1 的 1{,}176 条记录估计经典模型。图\ref{fig:p2_classic}左侧展示损失与两类规模的关系，右侧比较不同规模模型的残差，拟合得到
\begin{equation}
    L(N,D) = 1.6898 + \frac{0.3540}{N^{0.3400}} + \frac{1.2403}{D^{0.2799}}
\end{equation}
在 Pythia 族内，留出较大模型后预测仍较准确。将最小的 4 个模型用于重估参数，另外 4 个留作预测，检验结果为 $R^2=0.9999997$、MAPE $=0.005\%$。全量拟合的对应结果是 $R^2=0.9999998$、MAPE $=0.004\%$，其 RMSE 为 $1.5\times10^{-4}$。两种结果接近，说明所考察族内的规模关系较稳定。
\par
\end{samepage}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图1_经典标度律拟合.png}
  \caption{Pythia 的参数量、数据量与验证损失，以及分模型拟合残差}
  \label{fig:p2_classic}
\end{figure}

这组记录的损失标准差为 $0.342$，拟合残差标准差仅 $1.5\times10^{-4}$，二者相差较大。这样的吻合程度只对应当前来源，不能直接推广到其他训练资料。因此，下一步仍以 B4、B5 的外部记录及 B2 半合成轨迹作对照。

\begin{samepage}
还可从 B3 检查数据量的幂指数。对 8 条插值轨迹分别固定 $N$，扣除 $E+AN^{-\alpha}$ 后，将剩余损失 $L-(E+AN^{-\alpha})$ 对 $D$ 作双对数回归。指数落在 $-0.2806\sim-0.2803$，接近模型的 $-\beta=-0.2799$。由于 B3 由 Pythia 检查点插值得到，这只能说明轨迹内部一致，不能增加独立证据。
\par
\end{samepage}

\textbf{2.广义标度律参数估计}\par\nopagebreak[4]
再将 Pythia 的规模记录与 B6 质量实验合并，估计表\ref{tab:p2_form}选出的广义形式。对没有质量实测值的 Pythia 仍按约定取 $Q=1$，各项系数和指数列于表\ref{tab:p2_params}。

\begin{table}[H]
  \centering
  \caption{广义标度律联合拟合的系数与指数}
  \label{tab:p2_params}
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{5pt}
  \begin{tabularx}{0.94\textwidth}{@{}*{5}{>{\centering\arraybackslash}X}@{}}
    \toprule
    损失组成 & 系数符号 & 系数估计 & 指数符号 & 指数估计 \\
    \midrule
    不可约损失 & $E$ & 1.6558 & -- & -- \\
    参数规模项 & $A$ & 0.3808 & $\alpha$ & 0.3266 \\
    训练数据项 & $B$ & 1.2515 & $\beta$ & 0.2767 \\
    质量缺口项 & $C$ & 0.3544 & $\gamma$ & 0.9779 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.94\textwidth}\normalsize
  注：联合拟合 $R^2=0.9896$；不可约损失为常数项，不含衰减指数。
  \end{minipage}
\end{table}

\begin{samepage}
质量项的指数为 $\gamma=0.9779\approx1$，说明质量缺口增大时，渐近损失接近线性上升，参数见表\ref{tab:p2_params}。联合样本的拟合误差为 RMSE $=0.036$、MAPE $=0.66\%$，对应 $R^2=0.9896$。由于样本中新增了质量实验，不能拿它与经典拟合的 $R^2$ 直接相减，作为质量项收益的衡量。
\par
\end{samepage}

\begin{samepage}
图\ref{fig:p2_quality}说明质量对损失的影响。左图固定 $D=300$B tokens，用底色和等值线表示不同规模、质量组合的预测损失，空心点为 B6 实验位置；右图以 $E$ 为起点，展示质量不足使损失下限增加多少。
\par
\end{samepage}

从 $E+C(1-Q)^{\gamma}$ 可直接看出损失下限随质量的变化：满分参考 $Q=1$ 对应 $1.656$；若只达到 $Q=0.6$，下限为 $1.801$；降至 $Q=0.3$ 时，下限为 $1.906$。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图3_质量边际影响.png}
  \caption{固定数据量时的参数--质量损失等值线，以及质量缺口对应的损失下限}
  \label{fig:p2_quality}
\end{figure}

\textbf{3.分层检验与适用范围}\par\nopagebreak[4]
配置计算依赖模型预测，因此还需比较不同来源上的误差。表\ref{tab:p2_val}汇总 6 组检验的 MAPE、RMSE 和相关系数，图\ref{fig:p2_general}展示 $100(\hat L-L)/L$ 的分布：负值为低估，正值为高估。为同时看清小误差和大偏差，图中横轴在 $\pm10\%$ 内采用线性刻度，外侧改用对称对数刻度。

\begin{table}[htbp]
  \centering
  \caption{按数据性质区分的预测误差与相关性}
  \label{tab:p2_val}
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{5pt}
  \begin{tabularx}{0.98\textwidth}{@{}
    >{\hsize=0.75\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=2.25\hsize\linewidth=\hsize\raggedright\arraybackslash}X
    >{\hsize=0.75\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=0.80\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=0.75\hsize\linewidth=\hsize\centering\arraybackslash}X
    >{\hsize=0.70\hsize\linewidth=\hsize\centering\arraybackslash}X@{}}
    \toprule
    数据性质 & 对照样本 & 记录数 & MAPE/\% & RMSE & $r$ \\
    \midrule
    \multirow{2}{*}{真实} & B4 跨模型族收敛点 & 57 & 8.8 & 0.293 & 0.975 \\
      & B5 文献标度记录 & 44 & 8.0 & 0.193 & 0.910 \\
    \addlinespace[3pt]
    \multirow{3}{*}{半合成} & B7 新增质量实验 & 90 & 1.6 & 0.056 & 0.991 \\
      & B2 Cerebras 轨迹 & 1{,}029 & 33.7 & 1.245 & 0.666 \\
      & B8 校准段（方向变换） & 984 & 133.4 & 1.294 & 0.638 \\
    \addlinespace[3pt]
    估算 & B10 大规模估算损失 & 128 & 1.0 & 0.020 & 1.000 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.98\textwidth}\normalsize
  注：B8 使用 $1-Q_{\text{score}}$；B10 统一取 $Q=1$。B7 属于开发阶段验证，B10 不构成独立实验验证。
  \end{minipage}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图2_广义标度律验证.png}
  \caption{不同数据来源的有符号相对误差分布}
  \label{fig:p2_general}
\end{figure}

\begin{samepage}
表\ref{tab:p2_val}中，B4 的相关为 $0.975$，B5 为 $0.910$；相应 MAPE 仍有 $8.8\%$ 和 $8.0\%$。相关较高与预测偏差同时存在，说明模型捕捉到了一部分跨族变化趋势，绝对损失却不能同样准确地迁移。
\par
\end{samepage}

\begin{samepage}
对于同一半合成设定，B7 的 90 个新增点有 $1.6\%$ 的 MAPE，支持质量关系在该设定内作一定扩展。B10 的含义不同：损失本就由估算得到，比较时又统一设为 $Q=1$，所以只检查两种估算是否接近，不检验质量效应，也不属于独立实验验证。
\par
\end{samepage}

\begin{samepage}
B2 的 MAPE 达到 $33.7\%$；B8 校准段在翻转质量评分后仍有 $133.4\%$，部分损失甚至低于模型渐近下限。可见问题不止是分数方向；损失尺度和数据生成方式不同，也会使这些来源无法直接合并使用。
\par
\end{samepage}

\begin{samepage}
上述判断还受样本覆盖范围限制。B1 的参数约为 $0.07$--$12$B，数据量为 $0.134$--$299.893$B tokens；B6 的相应范围为 $0.07$--$11.97$B 和 $10$--$600$B tokens。质量关系主要来自 B6/B7 半合成实验，规模更大的估算记录只用于比较外推关系是否一致。
\par
\end{samepage}


\begin{samepage}
\textbf{4.大规模模型的对应与外推范围}\par\nopagebreak[4]
B9 提供大模型元数据，B10 提供估算损失，两者先按模型名称和模型族名称配对。B10 的 128 条记录都在 B9 中找到唯一对应，且每对的 $N$、$D$ 相同。B9 共 132 条，剩余 4 条的数据量为 0，又没有 B10 损失，故未进入对照。表\ref{tab:p2_large}按规模汇总了匹配结果。
\par\nopagebreak[4]

\begin{table}[H]
  \centering
  \caption{大规模匹配样本的覆盖范围与估算偏差}
  \label{tab:p2_large}
  \renewcommand{\arraystretch}{1.15}
  \begin{tabularx}{0.94\textwidth}{@{}>{\raggedright\arraybackslash}X*{3}{>{\centering\arraybackslash}p{0.19\textwidth}}@{}}
    \toprule
    参数规模区间 $N$/B & $[100,300)$ & $[300,1000)$ & $[1000,10000]$ \\
    \midrule
    成功匹配记录数 & 65 & 43 & 20 \\
    最小数据量 / B tokens & 1.3 & 0.3 & 0.1 \\
    最大数据量 / B tokens & $36{,}000$ & $30{,}000$ & $36{,}000$ \\
    MAPE / \% & 0.94 & 1.08 & 1.12 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.94\textwidth}\normalsize
  注：B9 元数据与 B10 损失按名称匹配；统一取 $Q=1$，MAPE 比较本文预测与 B10 估算值。数据量上下限为各组记录的实际极值。
  \end{minipage}
\end{table}
\end{samepage}

这批记录的规模为 $100$--$10{,}000$B，已经超过主拟合范围。分组误差约 $1\%$，表示本文预测与 B10 估算较接近；但 B10 同样由标度律产生，因此低误差不能代替真实训练检验，更不能说明质量项在这些规模下已经得到验证。


\textbf{5.质量投入的局部收益与规模替代}\par\nopagebreak[4]
下面选取两个参考配置，比较各项投入的小幅变化。表\ref{tab:p2_subst}同时列出弹性和质量--参数等效增量比，前者比较相同比例变化的影响，后者给出相同损失降幅下的局部换算。


\begin{table}[H]
  \centering
  \caption{两个参考情景下的局部敏感度与等效增量比}
  \label{tab:p2_subst}
  \renewcommand{\arraystretch}{1.18}
  \begin{tabularx}{0.94\textwidth}{@{}>{\raggedright\arraybackslash}X*{2}{>{\centering\arraybackslash}p{0.25\textwidth}}@{}}
    \toprule
    指标 & \makecell{参考情景一\\$N_0=1,\ D_0=100$\\$Q_0=0.6$} & \makecell{参考情景二\\$N_0=12,\ D_0=300$\\$Q_0=0.6$} \\
    \midrule
    参数量弹性 $\varepsilon_N$ & $-0.0491$ & $-0.0248$ \\
    数据量弹性 $\varepsilon_D$ & $-0.0383$ & $-0.0321$ \\
    质量弹性 $\varepsilon_Q$ & $-0.0838$ & $-0.0953$ \\
    局部等效增量比 $R_{NQ}$ & 2.844 & 76.838 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{0.94\textwidth}\normalsize
  注：$N_0$ 以 B 参数计，$D_0$ 以 B tokens 计；弹性无量纲，$R_{NQ}$ 的单位为 B/单位质量。弹性保留方向，图中展示其绝对值。
  \end{minipage}
\end{table}

\begin{samepage}
表中较小参考点 $(N_0,D_0,Q_0)=(1,100,0.6)$ 的质量弹性绝对值约为参数弹性的 $1.7$ 倍，同样小比例的改善中，提高质量对应的损失降幅较大。两个参考点的局部替代率分别为 $2.844$ 与 $76.838$ B/单位质量；若用一阶式代入 $0.1$ 的质量增量，分别相当于增加 $0.284$B、$7.684$B 参数。这组数值用于说明起点规模的影响，有限变化仍应按式\eqref{eq:p2_finite}计算，并检查拟合范围。它们表示损失效果相近，不表示成本相同。
\par
\end{samepage}

图\ref{fig:p2_margin}把参考位置的变化展开。前两图分别展示参数、质量的边际导数绝对值，由于单位不同，两图数值不能直接比较。第三图使用无量纲弹性，能够比较两个参考点上相同比例投入带来的影响。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图4_边际效用对比.png}
  \caption{参数量与质量的边际导数，以及两个参考配置的损失弹性}
  \label{fig:p2_margin}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图5_质量参数替代.png}
  \caption{三个目标损失下的规模--质量配置边界}
  \label{fig:p2_subst}
\end{figure}

\begin{samepage}
固定 $D=300$B tokens 后，还可以反求达到目标损失需要的质量，见图\ref{fig:p2_subst}。三个子图各对应一个目标，横轴为参数量，纵轴为质量；实线在主拟合范围内，虚线为外推。对于 $L=1.95$，图中特别放大了高质量区间，并将参数轴延至 $10^4$B 以展示理论边界，这不表示该规模在工程上可行。
\par
\end{samepage}

\begin{samepage}
接近质量满分时，导数还需特别注意。拟合得到 $\gamma=0.9779<1$，使 $|\partial L/\partial Q|$ 在 $Q<1$ 内逐渐增大，并在 $Q\to1^-$ 发散。因此，边际数值只在离端点较远的位置解释，不能直接用满分附近的导数建议实际投入。
\par
\end{samepage}

\begin{samepage}
\textbf{6.领域效应与配比衔接}\par\nopagebreak[4]
为观察领域之间的关系，图\ref{fig:p2_dom}左侧按 $d_{ij}=\sqrt{2-2s_{ij}}$ 对 17 个训练领域作平均连接聚类；右侧列出相似度最高和最低的各四对领域，用绿色和金色分别表示作用趋同和相反。该图比较的是各域影响验证任务的方式，并不检验混合使用是否产生额外增益。
\par
\end{samepage}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{../求解/问题二/图片/图6_领域替代互补.png}
  \caption{训练领域回归系数的相似性聚类与相似度最高、最低的领域对}
  \label{fig:p2_dom}
\end{figure}

在图\ref{fig:p2_dom}中，作用接近的例子有百科与网页、医学全文与医学摘要，相似度分别为 $0.925$、$0.903$；医学全文与邮件为 $-0.967$，则偏向不同的验证任务。能否替换还要把作用幅度和任务权重算进去。若从领域 $i$ 转移占比 $h$ 到 $j$，保证总量不变且 $0\leq h\leq p_i$，预测加权损失的变化为
\begin{equation}
  \Delta\widehat L_A=h\,\bm w^{\mathsf T}(\bm a_j-\bm a_i),
\end{equation}
$\bm w$ 为验证领域权重。上式为负，表示转移后总体损失下降；为零表示相当；为正则表示变差。相似度接近的两域仍可能有幅度差，方向相反也只说明任务取舍。由于没有建立交互项，不能从这个线性模型再推断额外的混合收益。


使用相同的验证领域和权重，比较问题一推荐配比与参考配比，线性模型给出的加权损失差为
\[
  M=\hat L(\bm p_{\mathrm{rec}})-\hat L(\bm p_{\mathrm{ref}})=-0.1988.
\]
\begin{samepage}
配比项 $M$ 使用附件 A 的任务和权重计算。把它放入广义标度律，还须假定配比效果可与规模、质量分开，且损失差能够跨来源迁移。前述检验已经显示来源间的偏差，因此 $0.1988$ 只能理解为当前条件下的改善量，不能保证改变规模后仍有同样收益。
\par
\end{samepage}

\begin{samepage}
在上述分离与迁移条件下，规模、质量和配比的关系可合写为
\par\nopagebreak[4]
\begin{equation}
  \begin{aligned}
    L(N,D,Q,\bm p)&=E+AN^{-\alpha}+BD^{-\beta}+C(1-Q)^\gamma+M(\bm p),\\
    M(\bm p)&=\sum_{i=1}^{17}(p_i-p_{\mathrm{ref},i})\,\bm w^{\mathsf T}\bm a_i.
  \end{aligned}
\end{equation}
\end{samepage}
在参考配比处有 $M(\bm p_{\mathrm{ref}})=0$。配比给定后，$M$ 是一个固定的损失平移量，不改变 $N,D,Q$ 的边际导数，也不改变等损失替代条件；本文的弹性均按参考配比计算。若再使用问题一的质量评分，还需要它与附件 B 的尺度能够衔接，这一点不能单凭加性写法保证。


本问建立的损失函数将用于下一问的预算配置。从计算可见，参数越多，继续扩容的边际收益越小；质量能等效替代多少参数，则要从具体起点计算。跨族和文献检验仍分别存在 $8.8\%$、$8.0\%$ 的 MAPE，故后续使用拟合参数时，也要一并保留质量尺度、数据来源和外推范围的条件。
\endgroup
