# 问题一修订决策与记录

日期：2026-09-25。基于开始本轮修订时的团队仓库提交 `63c174b`，工作分支为 `Q`。之后队友在 `main` 上继续修改问题二、三，本分支保留独立的修订基线。

## 本轮边界

按用户项目约定，先完成问题背景、问题一及其关联段落的文字和 LaTeX 修改。保留既有求解思路与计算结果。实质性模型问题交由用户决定；纯展示类图表调整先提出方案。问题二至四不在本轮处理范围内。

本轮使用 `humanizer` 的语言编辑规则，重点消除重复铺垫、机械衔接、冗长括注和不必要的强调。事实与证据的修订单独记录。

## 待决定 A：按既有方法修正描述与结论边界

建议采用下列保守修订：保留算法、数值、配比结果和图表数据，只使论文准确描述现有实现及证据能够支持的结论。

| 项目 | 依据 | 建议写入论文的内容 |
| --- | --- | --- |
| 预处理公式与实现不同 | `求解/问题一/问题一.py` 的 `orient_and_normalize` 使用 1% 与 99% 分位点并截断至 [0,1]；长度型指标在中心接近度变换后同样进行该缩放 | 将正文的最小值/最大值归一化改写为实际的分位点截断缩放；说明统计量由合并后的质量样本估计，缺失值在变换前填充。仅修正文档，不更换计算方法。 |
| 列表标量化说明不同 | `_softmax_expected` 对单元素直接取值，对多元素按 softmax 后的归一化级别取期望；`qurater` 也走这一函数 | 正文区分单元素和多元素列表；将表内“四项取均值”修为代码实际使用的期望级别。同时明确类别有序性仍需数据说明支持。 |
| “质量被配比吸收”推论不成立 | 领域评分固定时，混合质量向量满足 q̄ = Pq，本来就在配比矩阵列空间内 | “该质量项未增加独立的解释维度，因此增量检验不能识别独立的质量效应。”保留已有 R² 增量，并解释岭正则化会受冗余参数化影响。 |
| 冲突基准遗漏分布条件 | 2Φ(−τ/√2) 依赖独立标准正态分数 | 改称“独立标准正态参照”，报告观察值与参照值之差，不由此单独推断正相关。 |
| 跨尺度结论过强 | 现有结果为平均 Pearson 相关 0.782、0.651 | 改为“支持跨尺度变化趋势的关联”；绝对损失预测是否准确、系数是否可直接复用，尚需单独验证。 |
| 代表性结论过宽 | 抽样与扩展集对照只有 arxiv、github 两个共有域 | 结论限定在这两个域；表内差值为 0.0021、0.0006，正文不再写“均不超过 0.002”。 |
| 推荐配比解释过强 | 现有推荐来自线性模型和 2.5 倍上限约束 | 标明这是给定目标与约束下的预测推荐；部分领域降至零不能直接解释为这些领域没有训练价值。 |

用户决定：**先只润色，实质问题稍后讨论。** 因此，本轮保留以上原有公式和实质性结论；表中的修订方案仅作为后续讨论依据，不代表已经执行。

## 待讨论 B：暂不通过润色解决的实现问题

1. **冲突消解缺少实现证据。** 当前代码计算总体与指标对冲突率，并输出样本质量分数；尚未找到正文所述的逐样本多数裁决、冲突标记输出及下游剔除或降权实现。加权分数相对中位数的方向也不等于多数投票。需决定补做明确的消解方法，或如实界定现有结果并补充题目要求的处理方案。不得写成已经验证了消解效果。
2. **二分类方向存在歧义。** 二元素 softmax 期望实际取列表第二个元素的概率；论文却称第 1 类对应流畅或无广告。这里可能是类别编号从 0 开始造成的命名歧义，也可能是方向错误，需要原始字段说明或样本证据才能定论。未经核实不翻转指标、不重算质量评分。
3. **PCA 对照名实不完全一致。** 代码对第一主成分载荷取非负部分、归一化，再对归一化指标加权。这与直接使用第一主成分得分不同。需确认按实际实现更名，或重做标准 PCA 对照；本轮不擅自替换对照算法。
4. **推荐损失的基线需核对。** 代码用于打印参考损失的 `obj(p0m)` 会经过 `build` 重新归一化，未必等于直接用参考配比 `ref` 得到的损失。已有“5.336 → 5.136”的基线与下降量应先局部核对，再决定是否更正。无需全量训练或重跑质量信号。
5. **题目材料定位。** 当前已定位团队仓库和 `E:/华为杯f/real_attachments`；尚需定位用户所述的独立题目 DOCX 与《数据说明.pdf》，才能完成逐字覆盖检查。论文自身的“问题重述”不能替代原题。

## 待决定 C：新增图的具体方案

### 图 1：总体建模架构图

拟放入“总体分析”。以附件数据、建模任务和输出三层组织，问题一突出质量信号和配方损失两类输入，输出综合质量、冲突诊断和配比预测。其他问题仅作为现有全文结构的概括性节点展示，不修改其内容。箭头仅表示核实后的实际数据或参数关系，不加入尚未验证的衔接。

### 图 2：问题一求解流程图

拟放入问题一“具体分析”之后，采用左右两条流程：

- 左侧：A1–A3 质量信号 → 缺失处理与标量化 → 指标方向和缩放 → 熵权综合评分 → 样本、语料及领域汇总；关联显示冲突诊断。
- 右侧：A4–A5 配方与损失 → 索引匹配 → 岭回归 → 1M 验证与跨尺度趋势检验 → 有界配比推荐。
- 中部通过 A16 领域映射连接，标出混合质量指数的构造；不把尚未实现的消解、剔除或降权画成已完成步骤。

两图均使用蓝灰配色、统一字体及矢量绘制。拟交付可编辑 TikZ 源文件，图中不加入新的实验数据。

用户决定：**两张都加。** 已以 TikZ 源文件实现并接入正文。总体图按各问展示输入、方法与产出，不增加未经核实的跨问参数传递连线；问题一流程图只展示已有步骤。已有结果图本轮保持原样。

## 修改与验证记录

| 项目 | 当前状态 |
| --- | --- |
| 同步修订开始时的 GitHub 版本并建立 Q 分支 | 已完成，起点为 63c174b；原仓库 Q 分支已建立 |
| 文字与结构修改 | 已完成首轮：引言背景、总体分析的问题一说明、问题一正文和摘要的方法叙述；补充问题一符号 |
| 模型、算法和计算结果 | 按用户决定保留；未运行求解脚本 |
| 决策 A、C | A 暂缓；C 两张图均已获准实施 |
| 问题二至四 | 不在本轮范围内 |
| 编译与版面核验 | 已完成：XeLaTeX 编译成功，125 页；新增图、问题一长表及末图完成页面检查 |


## 其他核查记录

- 代码未见普通最小二乘或随机森林的完整对照实验，但论文有“比较”表述。按本轮决定暂保留，后续需补充证据或修正描述。
- 已查阅 [OpenScholar 原始论文](https://www.nature.com/articles/s41586-025-10072-4)。该系统包含检索增强和反馈机制，不能仅凭系统比较结果断言优势来自数据质量与配比。原背景段的这一解释留待实质问题讨论，本轮只重写前后两段。
- 配比回归的逐元素下标与矩阵维度、混合质量项的矩阵写法仍需核对；本轮只拆分长公式的显示布局，未改动表达式。
- 用户提到的独立题目 DOCX 和《数据说明.pdf》未在已检索的项目、桌面及下载目录中定位到。本轮不能据论文自身的重述宣称完成了原题逐字覆盖检查。

## 编译说明

新增 GitHub Actions 编译流程使用 XeLaTeX。Linux 环境没有 Times New Roman、Arial 或楷体时，工作流只在临时检出目录中使用 TeX Gyre / Fandol 字体回退，不改动仓库的 `format.cls`。中文仍沿用模板随附的思源宋体；因此线上预览与具备原字体的 Overleaf 环境可能有少量字形或换行差异。

本轮新增图采用可编辑 TikZ 矢量源文件，没有重跑模型或更换已有结果图的数据。保留现有结果不代表上述疑点已获核实。

### 首次交付核验

- 编译源提交：`b6f97694f847113d37077b4e4e1d43179b9f1130`；[GitHub Actions 构建 36112603556](https://github.com/kannyshan2333/-F-/actions/runs/36112603556) 成功，产物 `paper-preview` 编号为 `10853333490`。随后仅更新记录文档及编译 PDF。
- 首次交付时，仓库中的 `论文/论文.pdf` 更新为上述构建产物，共 125 页。按 PDF 文件页序复核第 7、10、11、12、20 页，确认两张新增图、长字段表格和问题一末图没有遮挡或裁切。
- 问题一对应编译日志无 `Overfull` 警告；全文交叉引用未发现未定义项。其他章节及附录仍有原有排版警告，未纳入本轮修订。
- 对照 `63c174b`，静态核对问题一数学表达式、六张表的单元格内容、原有结果图引用、摘要结果段均保持一致；问题二至四正文、模型假设、模型检验、模型评价和 `format.cls` 未修改。
- 可编辑图源位于 `论文/figures/overall-architecture.tex` 与 `论文/figures/q1-workflow.tex`，随主文档编译为矢量图。

### 图表标题的预览兼容修复

用户在 Codex 内置 PDF 预览中发现表题中文缺失，表 2 只剩“2 22”，表 3 只剩编号、A1–A3 和样本量。源码与 PDF 文字提取均包含完整标题，Poppler 渲染也正常；此前仅检查该渲染结果，未发现这一兼容问题。

旧表题通过模板的 `song` 标题样式调用 `\songti`，编译后使用 FandolSong 字体；正文使用随项目附带的 Source Han Serif。离线 PDF.js 对照复现了相同现象：未提供 `cMapUrl` 时，FandolSong 加载失败，两张表的中文标题消失；提供 CMap 资源后恢复。该对照确认旧表题依赖外部中文字体映射。

修复为定义 `bundledsong` 标题字体并指向项目已有的 `\song`，仅在总体架构图及问题一局部分组内启用。保留标题文字、字号、粗体、表格内容和数值；`format.cls` 与问题二至四源文件保持原样。

源提交 `f577ceaeb0618502569891ace85d1b0ccc424fac` 的 [原仓库构建 36137115397](https://github.com/Zhouzehua123/-F-/actions/runs/36137115397) 成功，产物编号为 `10864832563`，新版 PDF 仍为 125 页。已将该产物更新到 `论文/论文.pdf`。

在同一离线 PDF.js 环境中，不提供 CMap 资源重新渲染 PDF 第 11、12 页，两张表及续表的中文标题均完整显示，文字提取也通过；PDF 字体核对确认标题已改用 SourceHanSerifCN。问题一日志无溢出警告，未发现未定义交叉引用。本次未直接读取 Codex 内置预览画面，验证依据为用户所见缺字现象的复现与同条件复测。

## 本轮已实施的排版修复

- 将配比回归的逐元素形式、矩阵形式、目标函数和系数估计分开显示，保留原有数学表达式。
- 为问题一的六张表局部设置列间距、字号和外边距，并在长字段名的下划线后提供换行位置。所有单元格文字与数值保持不变。
- 在问题一范围内约束段首、段尾的孤立行，避免参考文献序号单独落在下一页。
- 为问题一末图安排完整页面空间，修复其被挤出页脚的问题；原图文件、尺寸设定和数据均保持不变。
- 图形排版经过 PDF 页面检查后调整节点文字换行与间距，修复架构图中节点过密的问题。

## 协作入口

已使用用户指定的 `kanny2333` 账号确认写入权限，并将修改推送至团队原仓库的 [Q 分支](https://github.com/Zhouzehua123/-F-/tree/Q)。后续以该分支为协作入口。

此前建立的 [PR #1](https://github.com/Zhouzehua123/-F-/pull/1) 以 `kannyshan2333:Q` 为来源分支。用户已明确同意将 Q 合入 main；合并记录见文末。

## 2026-09-25：表内说明改写

用户要求将表内解释一并纳入文字润色。本次沿用“仅润色，实质问题另议”的决定，使用 humanizer 检查表述，改写问题一六张表的说明、表头和表题，以及符号表中与问题一有关的 13 条解释。专业名称、字段标识、符号、单位、权重和结果数值保留；未运行求解脚本。

### 与团队版本同步

截图中的三列表格来自主分支。先以合并提交 `cb544e0` 将主分支 `a3aa018` 同步到 Q，再单独提交表内措辞修订，便于区分团队更新和此次修改。

- 采用主分支的“符号、含义、单位”三列结构，保留主分支对问题二至四符号的定义及单位，补回 Q 原有的五组问题一符号。
- 问题二至四正文、求解文件和相关图源与 `a3aa018` 一致；摘要中问题二至四的内容也与该版本一致。
- 保留 Q 已有的背景及问题一润色、两张已批准的流程图和标题字体兼容修复；接入主分支的全局排版设置、标题预留空间和配比回归引文更新。
- 问题一表格沿用全局正文字号，不再单独缩小字号。此前格式自查报告针对旧版 PDF，其中的页码和版面判断不能直接套用于合并后的版本。

### 表述示例

本次逐表处理范围如下：

| 表格 | 此次改写内容 |
| --- | --- |
| 符号说明表 | 与问题一有关的 13 条解释；问题二至四专用符号沿用主分支 |
| 指标方向与熵权表 | 19 项指标解释、表头和表题；另 3 项涉及已登记疑点，暂保留 |
| 缺失统计表 | 表头、缺失情况说明和表题 |
| 赋权方案对照表 | 表题与相关系数表头；方法名称和计算结果保留 |
| 抽样与扩展集对照表 | 表题与评分、领域映射表头；领域名称和数值保留 |
| 冲突统计表 | 表题与统计范围、记录数和冲突比例表头；数值保留 |
| 配比推荐表 | 表题与训练占比、变化量表头；领域名称和数值保留 |

| 位置 | 原表述 | 修订后 |
| --- | --- | --- |
| 符号 N | 模型参数规模 | 模型包含的参数数量 |
| 符号 D | 训练数据规模 | 用于模型训练的数据总量 |
| 符号 Q | 数值越大质量越高的数据评分 | 数据质量评分，分值越高则质量越好 |
| 符号 p | 分量非负、总和为 1 的领域配比向量 | 各领域训练占比构成的向量，各项非负且总和为 1 |
| 指标含义 | 可读性 | 文本的易读程度 |
| 指标含义 | 书籍域重要性权重 | 相对于书籍领域的重要性权重 |
| 缺失情况 | 整行 6 维取值均为 NaN | 该记录中此字段的 6 个分量均为 NaN |
| 缺失情况 | 无缺失 | 未发现缺失值 |
| 赋权方案表头 | 域级 Spearman 排序一致性（对熵权法） | 领域排序相关性；Spearman 系数，参照熵权法 |
| 配比推荐表头 | 调整量 | 占比变化量 |

指标表中 fluency_en、ad_en 的类别方向及 qurater 的聚合方式涉及已登记的实质疑点，暂保留原说明，等待后续讨论。Kendall 协同系数、熵权、均方根误差等标准术语不作无依据的同义替换。本次未进行查重检测，不据改写数量推断查重降幅。

### 核验

静态核对通过：六张表的字段、数据类型、评价方向、权重和结果数值一致；符号表的符号、单位及常数一致；问题一公式和两张已批准图源一致。

- 最终源提交：`14c5d1d160895f4709828a82ff6379617d2b9750`。[原仓库 XeLaTeX 构建 36150503698](https://github.com/Zhouzehua123/-F-/actions/runs/36150503698) 成功，产物编号 `10871396823`。
- 仓库 PDF 已更新为该产物，共 60 页，SHA-256 为 `D46F30D9421263A2545812D846B55AAB7F23E64EEA21611BE494D43B1DBA0D52`。总页数变化主要来自同步的主分支排版和附录整理，不能理解为仅改写表格便从 125 页缩减到 60 页。
- 已检查符号表与六张问题一表格所在页面；最终轮重点复核 PDF 第 6、8、9、10、12、13、16 页。缩短表头后，“类型”“方向”不再出现拆字换行，赋权对照表的长表头分成两行，抽样与扩展集对照表可完整排在第 12 页。
- PDF.js 不提供外部 CMap 资源时，最终稿第 6、8、9、10 页的符号表、指标表及续表、缺失统计表标题均能完整显示中文。此项为离线同条件复测，未直接读取 Codex 内置预览画面。
- 最终编译日志中，符号表和问题一没有 `Overfull` 警告，全文未发现未定义交叉引用。问题三仍有 3 处原有横向溢出警告，保持主分支内容，本轮未处理。
- 未运行求解脚本或查重检测；未因编译和局部页面核验通过而宣称旧版自查清单中的全部问题已解决。

## 2026-09-25：合入 main 并同步协作分支

用户明确要求将 Q 合入 main、解决其他分支的冲突，并暂不渲染 PDF。

- 合并前远端状态：main 为 `a3aa018`，Q 为 `fba823e`，syq 为 `51aea0c`。Q 已包含最新 main，syq 的全部提交也已包含在 main 中，因此本次没有需要人工选择内容的合并冲突。
- 采用合并提交保留 Q 的完整历史，并将协作分支同步到合并后的 main。使用普通推送，不重写提交历史。
- 合并前后均核对问题一六张表的数据、符号单位、公式和已批准图源；问题二至四及求解文件仍与原 main 一致。
- 本次不运行求解、不编译或渲染 PDF，合并提交使用 `[skip ci]` 跳过自动编译。仓库沿用源提交 `14c5d1d` 对应的已验证 PDF。


## 2026-09-26：同步最新团队版本并调整六张表的字段顺序

本次先获取远端，融合 main 的 `35f6c92`（最新全文润色）及 origin/Q。七处冲突均为问题一图表说明的交叠：保留队友的新表述，补回与已确认图形一致的图例和统计量解释；保留新版图题及宽度，并使 A/B 分面引用与图一致。符号表继续保留两列。

用户要求调整问题一各表字段顺序；本次只移动列，所有行顺序、字段含义和原值均保留。新顺序如下：

| 表标签 | 字段排列 |
| --- | --- |
| `tab:p1_dir` | 指标字段 → 指标含义与判向依据 → 类型 → 方向 → 熵权 $w_j$ |
| `tab:p1_missing` | 缺失字段 → 缺失情况 → 涉及记录数 |
| `tab:algo1` | 赋权方法 → 与熵权得分的相关系数 → 领域排序相关性； Spearman 系数，参照熵权法 → 样本平均得分 |
| `tab:p1_qa` | 质量信号领域 → 训练配方对应领域 → A1 评分 $Q$ → 扩展集评分 $Q$ → 评分差值 |
| `tab:p1_conf` | 统计范围 → 记录数 → 冲突比例 $\rho_c$ → 独立基准 $\rho_0$ → $\rho_c/\rho_0$ → Kendall $W$ |
| `tab:p1_rec` | 训练领域 → 推荐占比 → 参考占比 → 占比变化量 |

同步调整了普通表头、续表表头、数据列及对应列宽。22 项指标行序保持不变，M01–M22 编号不受影响；评分差仍为 A1 减扩展集，占比变化仍为推荐减参考。逐表按原列索引反向置换核对，字段和值完全相同；调整前后六张表以外的正文相同。未重新运行求解程序，也未改动模型或结果。

本地编译发现推荐配比表被积压的浮动图隔开，最后一行单独落到下一页。已在该段落前清空待排浮动图，并预留 22 行版面以容纳标题、说明与整表。这是表格分页修正，正文文字、图片内容与数据不变。

核验完成：6 张表、48 行、224 个数据单元格逐格置换一致；12 个表头及字段列宽正确。XeLaTeX 本地编译成功，PDF 共 97 页，无缺字、Overfull 或未定义引用。实际查看六张表及续表所在页面；推荐表 10 行已完整置于物理第 20 页。此次没有重跑求解。
