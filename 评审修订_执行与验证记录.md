# 全文评审后的修订与验证记录

> 下文是 a784920 阶段的历史记录。纳入最新主线模型后的结论、85项复现与排版结果，请以《全文复核_主线整合与交付.md》为准。

## 执行依据

2026-09-27，用户在收到全文评审与修订路线图后授权“发现的问题能改的就改”。本次从团队最新main `90bedde`建立独立分支`codex/review-fixes`。

本次采用已有路线图的保守修订方案：纠正确定错误，补充现有数据支持的诊断，将超出证据的说法改成准确的条件分析。保留原始附件、主模型、原拟合参数和正式配置结果；独立诊断另存，不静默替代论文的主结果。涉及主推荐规则、评分映射、质量函数或因果识别设计的更换，列出依据及剩余选择。

## 计划与文件职责

- [x] Q1：修正字段释义；补相同优化目标下的预测/选优检验、代理分歧、配比敏感性；明确冲突标签及评分标尺。文件：`论文/5.1.问题一.tex`、必要图标签及`求解/审稿补充/q1_决策与稳健性.py`。
- [x] Q2/Q3：明确质量映射假设、参考配方下界及可分离条件；补B6组内质量效应、gamma端点和M幅度诊断。文件：`论文/5.2.问题二.tex`、`5.3.问题三.tex`、独立诊断程序。
- [x] Q4：准确命名横截面关系、规模关联项/剩余项、曲线分位区间；补窗口和时间留出检验；同步错误图注及图中文字。文件：`论文/5.4.问题四.tex`、Q4绘图及独立诊断程序。
- [x] 全局：统一摘要、总体分析、符号、假设、检验和优缺点；修正MAE、实际样本数及图源说明，关闭已修复的Kendall/Pq旧意见。
- [x] 工程：为复现入口先编写失败用例，覆盖“零输出假通过”、缺输出、输出不一致和JSON；加入预期派生输出的重生成门槛，不改动主模型算法。
- [x] 验证：单元核验、独立诊断回代、原结果/求解逻辑不变核对、图源与引用检查；隔离执行四问，稳定编译并目视检查受影响页面。
- [x] 同步：合并远端main并解决冲突，保留main合并边界；最终普通推送状态随交付回复记录。

## 复现检查的设计

在临时副本运行模型前，清除**清单指定的本问派生输出**，保留作为输入的其他问结果。运行后确认所有预期CSV/JSON生成，才逐项比较；缺失、新增且无基准、未比较不能写成相同。清单必须以明确相对路径定义，清理目标限制在隔离副本的求解目录内。若进程失败或比较失败，保存部分报告并返回失败。小型测试用模拟运行回调验证这条控制链，不消耗一次真实全模型求解。

## 已实施的修订

1. 问题一新增四张诊断表，分别核对同一加权目标下的排序、两个代理对同一候选的预测、正则变化下的系数与配比、A1 与全量记录建立的评分标尺。原方案保留为“线性条件候选”，不再借用核岭的预测精度证明其训练收益。冲突标签明确为域内相对位置，不声称完成了质量真值裁决。
2. 问题二、三新增质量映射、固定 gamma=1、配比收益折减三张表；说明 B6 组内质量收益随规模变化，区分参考配方渐近基准 E 与 E(p)，写清固定 Q0 和可分离优化成立的条件。29.6 倍保留为固定绝对损失平移的模型算例，并补充其对假定收益幅度的敏感性。
3. 问题四新增年份处理、前沿窗口、事后时间留出三张表；改称合并横截面回归、规模关联项与未解释剩余项；重抽样结果准确称为曲线重拟合分位范围。原点预测保留，但明确两类预测的基期水平不同，以及末端平台对饱和预测的影响。
4. 同步摘要、假设、符号、总体分析、检验、模型评价、附录及总体/问题一/问题四流程图。纠正全量经典模型 MAE 为 1.1e-4、广义联合 MAE 为 0.017、C3 有效回归样本为 4579。问题四分解图引入句与图中实际内容对应。
5. 纠正五项质量指标的词项/字符/行释义。问题一主程序只改这五个说明字符串，并同步“质量指标熵权.csv”的文本说明列；数值、方向、归一化及熵权计算均未变。这一文本修正单列记录，不能把该 CSV 误报为字节未变。
6. 复现入口改为明确核对 81 项预期 CSV/JSON：运行前移除隔离副本中的旧输出；缺项、未重生成、差异和进程错误均失败；正式 CLI 还核对四问及结果覆盖。JSON 数值采用与 CSV 相同的容差，环境元数据单独记录。微型回归测试覆盖零输出假通过、部分缺失、数值差异、进程异常、非法路径和漏项清单。

本轮新增诊断脚本及其结果位于 `求解/审稿补充`。新的表格均有独立计算结果，不以文字替换代替数值核验。

## 评审意见的处理状态

| 原意见 | 本轮处理 | 仍然需要的证据或选择 |
|---|---|---|
| Q1-M1 代理预测与配方选优不一致 | 补目标排序、候选双代理、凸包距离；保留条件候选 | 实际推荐仍需候选区域实验或重新选择响应模型 |
| Q1-M2 精度稳定不等于系数/份额稳定 | 补四档系数与配置敏感性；修正总表 | 不将详细百分比解释为唯一可靠配置 |
| Q1-M3 冲突裁决有效性不足 | 明确标签的相对定位含义及近乎全覆盖事实 | 真实消解效度需要独立人工/任务依据 |
| Q1-M4 A1 与全量标尺 | 补同公式、不同标尺对照；保留主评分 | 更换主标尺涉及原数值和跨问基线，未擅自替换 |
| Q2/Q3 质量尺度缺少标定 | 明示恒等映射假设并补单调映射反例 | 实际部署需同批语料的双尺度标定 |
| Q2/Q3 gamma 端点敏感 | 补受限拟合及原饱和预算比较 | 主函数及参数保留；未伪造参数置信区间 |
| Q2/Q3 配比与质量基线耦合 | 明示共同 Q0 情景，补候选自身基线对照 | 若采用配比相关成本，需另建联合模型 |
| Q2/Q3 绝对损失平移迁移 | 补迁移幅度表，明确 E(p)；限定 29.6 倍 | 跨规模绝对收益仍缺少标定 |
| Q2/Q3 质量与规模交互 | 报告 B6 固定规模内的质量收益差异 | 是否引入交互项、替换主模型须另行决定 |
| Q4-M01 年份组差当作动态增长 | 修正统计对象、表头及全局表述；补编码诊断 | 结构外推仍为额外年增量假设 |
| Q4-M02 剩余项当作技术贡献 | 同步公式、图注、图中文字与摘要 | 未增加因果识别设计，不作因果归因 |
| Q4-M03 累计平台决定饱和预测 | 补窗口删减及时间留出表 | 原预测保留为窗口条件情景，尚无可靠未来验证 |
| Q4-M04 曲线带与观测覆盖混淆 | 全文区分曲线分位范围与观测预测区间 | 未声称已获得 90% 的未来覆盖率 |
| CODE-COVERAGE-01 复现假通过 | 修复生产门槛并加入回归测试 | 实际重算结果见后续验证记录 |
| Q4-FIG-INTRO-01 图文不对应 | 引入句改为两窗口增量核算 | 已按图的内容纠正 |

“已限定适用范围”不等同于“新增了支持原强结论的证据”。上述模型选择与标定缺口仍保留，原始数据和主要数值结果没有为改善表观结论而修改。

## 验证与交付

### 文件保留与静态核验

- 与本轮起点 `90bedde` 对照，四问主计算逻辑保持不变。问题一只纠正五个指标说明字符串。
- 81 项已保存主输出的数值与 JSON 内容均未替换；仅问题一熵权 CSV 的五处说明文字更新。45 项原始输入文件 SHA-256 与原清单一致。
- 18 个有效 TeX 源文件、93 个标签、27 项参考文献已核对，无重复标签、缺失引用或缺失引文；31 张实际引用图片更新来源记录。
- 原有带“面板”“裁决”等字样的结果文件名保留，便于兼容现有读取入口；论文及图中的统计含义已更正，不能把历史文件名当作新的方法定义。
- 本轮证据见 `求解/审稿补充/本轮修订核验.json` 与 `求解/图表来源.json`。历史 `求解/复现清单.json`、`求解/源码静态等价核验.json` 保留原审查时间。

### 实际运行与未通过项

复现入口的 9 项回归测试通过。完整求解在独立副本中执行，四个入口均运行完成，81 项预期输出均重新生成并比较，源仓库数值结果未被覆盖。

**严格比较结果为 80 项一致、1 项不一致，整体 `passed=false`。** 不一致文件为 `求解/问题三/结果/预算扫略.csv`，涉及 141 个预算中的 14 个、3 个字段及 16 个单元格。比较门槛保持 `rtol=1e-6, atol=1e-8`，没有为了通过而放宽。

对差异预算进一步以独立剖面法核对：所有受影响解均满足原边界，最大预算相对残差为 `7.31e-14`；在同一参数下，新旧配置的损失差不超过 `4.00e-15`。近零质量费用由浮点末位残量放大，少数派生比值略超相对容差。这支持数值停止精度和边界浮点漂移的解释，不支持据此判定主模型错误，也不把严格检查改报为通过。原保存损失与现共享参数代回还有 `1.63e-9` 量级末位差，历史形成过程不能凭现有文件确定。

完整状态与实际环境见 `求解/审稿补充/完整复现比较.json`，逐项差异、约束检查、参数差异和可重复诊断程序见 `求解/审稿补充/q3_复现差异/`。诊断用输入与最终完整运行的对应输入已核对哈希一致。

本次使用独立 Python 3.13.12 环境，实际版本列入运行报告。仓库既有依赖记录保留，未把本次环境冒称为旧结果的原始运行环境。

### 编译与视觉检查

合并后的稳定源文件已通过 XeLaTeX/latexmk 编译，更新 `论文/论文.pdf`，共 180 页（包含封面与代码附录）。编译日志无 LaTeX 错误、未定义引用、缺失引文或 Overfull 内容越界。保留封面 PDF 版本、封面页锚点及 Underfull 等非阻断警告。

已目视检查新增诊断表、修正图、总体流程图及合并后发生分页移动的页面，未发现中文表题缺失、表格溢出或图文遮挡。编译证据及源文件/PDF 哈希见 `求解/审稿补充/PDF编译核验.json`。

### 多人同步与交付边界

修订提交为 `9c7b118`。推送前发现团队 main 更新至 `2fc1b10`，已合并为 `7a87cce`。逐段解决问题一、问题二及附录的冲突，保留队友减少留白、释放段落锁定和图文位置调整，同时保留本轮诊断、准确术语及条件说明。

修订分支 `codex/review-fixes` 已推送至 `a784920`。首次交付时保留独立分支；随后用户明确要求“做完push 合入main”。按此授权重新获取远端状态，以最新 main `2fc1b10` 合并该修订分支，本地先生成4f34f38，推送前发现远端主线已新增模型修改，因此没有推送该中间版本；转入主线整合分支继续审核。最终同步状态见《全文复核_主线整合与交付.md》和Git历史。

本轮不能补足尚未进行的实证标定：质量尺度映射、候选配方真实训练收益、跨规模绝对收益迁移及未来预测效度仍应另行研究。正式完整题面材料尚未补齐，不能把本轮合理性审查等同于对原题每一项要求的最终验收。
