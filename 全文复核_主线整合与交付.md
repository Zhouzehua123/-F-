# 全文复核、主线整合与交付

## 范围与版本

- 依据用户授权审查全篇并修正可确定的问题；对模型更新同时核对算法、绘图、表格、文字及附录。
- 本轮纳入团队主线 `529d586b6537b154eb44103d4e90a59a60d76282`，包含 `29d6706` 的核岭推荐、前沿模型配对修改，以及后续附录公式、单位、标题调整。
- 独立整合分支为 `codex/review-main-integration`。本地原有审稿修订没有被覆盖，按段处理模型接口与文稿冲突。
- 原始附件不改。主线正式模型及保存数值均保留，新增核验另存；只修复复现检查程序的缺陷和确定的说明错误。

## 逐项同步

| 内容 | 算法及结果 | 图表及正文 |
|---|---|---|
| 问题一正式推荐 | 沿用主线核岭在训练凸包内求解；旧线性方案读取独立对照表；检查48个凸组合权重、固定域及上限 | 正式候选与旧线性诊断明确分开；更新求解流程、10张图及来源哈希；保留同目标选优、正则敏感性和A1标尺对照 |
| 问题一指标释义 | 更正5项词项、字符占比说明；评分数值、方向与算法不改 | 熵权表、绘图读取代码与正文同步，不把词2-gram写成字符2-gram |
| 问题二、三接口 | 核岭配比损失差为−0.1729738512，固定共同Q0；重新计算独立诊断 | 条件算力倍数统一为16.9074；区别候选自身质量、共同基线及质量成本；不把模型内换算解释为实测提速 |
| 问题四前沿 | 沿用主线评分与对应模型规模配对；19项主输出未被诊断覆盖 | 长窗口规模关联/剩余项统一为51.4%/48.6%；重绘分解图，删除旧错配解释；保留时间窗口与三截点检验 |
| 全篇逻辑 | 核对参数、跨问接口、附录公式、数据来源与真实/半合成/估算性质 | 摘要至评价统一术语；改正预测基点、筛选与分组、熵权层次等6处歧义；不把数值保护梯度间隙称为驻点证明 |
| 复现入口 | 85项明确清单；检查源与重算的核岭证书；拦截缺项、NaN/无穷、重复初值、非法布尔、异常残差 | 更新附录运行说明；错误仍返回失败，不通过放宽容差掩盖差异 |

## 实际核验

- 复现程序17项回归测试通过，包含实际核岭记录的反例，非单纯模拟成功路径。
- 在隔离副本重新运行四问，85项输出全部生成并比较，84项通过、1项严格比较未通过，整体 `passed=false`。源结果哈希保持不变。
- 不通过项为问题三 `预算扫略.csv`：14个预算、16个单元格，涉及 `D_over_N`、`c_qual`、`f_qual`。沿用 `rtol=1e-6, atol=1e-8`。
- 独立消元剖面核验显示所有受影响配置满足边界，最大预算相对残差7.31e−14；同参数评价的新旧目标值差不超过4.00e−15。严格差异保留为浮点与停止精度风险，不改报全通过，不覆盖主线结果。
- 静态147项断言通过：7份核心算法及依赖AST、85项保存输出、45份附件、31项图源；18份活动TeX中123标签、27参考文献和26代码输入完整。
- 主线补充检验重新运行，核岭预测重建最大差3.87e−12，C3回归有效记录4579，源正式结果未改。
- 新环境为独立Python 3.13.12，具体软件版本见完整复现报告；不冒称与历史运行环境相同。

## 文字与降重

采用 `academic-humanizer-zh` 做学术保真改写：删除重复接口介绍，合并两版重复的选优和时间留出表，明确主语、数据来源与推断条件；保留必要术语、公式、数据、引文及不同模型的限制。没有通过替换同义词改变数学含义，也没有为了降低表面相似度省略来源。

本轮没有重新提交商业查重服务，因而没有可报告的新重复率，也不承诺某一检测分数。旧稿匹配应与外部来源重复分别解释。

## 排版核验

使用 `latex-thesis-zh` 的编译包装脚本，实际执行XeLaTeX/latexmk。初版渲染发现正文物理第21、25、46页较大留白，原因包括强制定位流程图、末尾浮图与屏障的组合。仅调整四处图的浮动参数，保留尺寸、字号、数据和图文内容；随后重新编译、逐页检查。

通用 `check_format.py 论文/论文.tex` 返回1（WARNING），报告293个中英文混用候选，ChkTeX未安装。此结果属于源码启发式提示，包含数学公式、英文变量和参考文献，不能据此宣称竞赛格式自动验收通过；实际分页、中文表题、图文遮挡及越界以最终PDF和编译日志为准。单位列继续遵守用户此前删除的明确要求。

最终PDF共211页，含长代码附录，附录从物理第66页开始。已查看正文及附录起始的67页缩略图，并放大复核第25、46、58、59、61页；未见表题缺字、图文遮挡或越界。编译无未定义引用、缺失引文或Overfull。页数、截图页、源文件与PDF哈希记录于 `求解/审稿补充/PDF编译核验.json`；标签、引文、原始输入、保存输出及图源记录于 `本轮修订核验.json`。历史81项/180页证据单列于 `历史核验_a784920`，不与本轮混用。

## 仍须保留的学术边界

1. 新配方在核岭代理中改善目标，但没有新增训练实验，不能宣称真实训练收益已经验证；多初值一致也不证明全局最优。
2. A、B质量尺度的对应、跨规模绝对配比收益仍是条件假设；敏感性分析不能替代实证标定。
3. 冲突标签表示相对位置，缺少独立真值时不能称作已完成有效裁决。
4. 前沿外推没有稳定战胜时间留出持平基线；重拟合曲线分位范围不是未来观测覆盖保证，剩余增量也不是已识别的技术因果贡献。
5. 正文逻辑和已有附件已经核查；完整原始题面材料未补齐的部分不能声称完成逐字答题验收。

## 同步与合并

合入主线和每次推送前均重新fetch，普通推送，保留队友历史。最新同步版本、最终提交与远端核验以交付回复和Git记录为准。
