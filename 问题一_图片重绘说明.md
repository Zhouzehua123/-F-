# 问题一图片重绘说明

## 目标和范围

用户要求重绘问题一全部图片，并参照 Q2、Q4 的现有风格。问题一共有 9 幅编号图，即正文图 2—图 10；其中 8 幅为结果图，1 幅为求解流程图。总体分析中的架构图属于另一章节，本次不改。

以合并后的 `8ca02a9` 为起点，在 Q 分支实施。完成前已同步主分支 `335672a` 中队友提交的 Q4 新版，采用快进同步，无冲突。本次自己的修改仅涉及绘图方式、配套图注和直接描述图形的语句，不改模型、原始结果或问题二至四。此次输出 PNG 和 SVG，不重新编译整篇 PDF；仓库原有论文 PDF 因而仍是上一次编译的版本。

## 风格依据

参照问题二的重绘图和问题四的“逐任务聚合图”：灰紫 `#665080`、青绿 `#367E73`、金橙 `#CB8B38`，白底、浅网格、左对齐分图标题，中文宋体、英文 Times New Roman。完成前又核对了主分支 `335672a` 中问题四的七图新版，其主色也为上述三色，已与本次 Q1 重绘保持一致。

这组数据主要比较不同领域，类别之间没有连续顺序。使用点图、热图、气泡矩阵和分布图；连接线仅连接同一领域在两种设定下的值。流程图采用 Q2 的浅底卡片布局。误差线仍表示原结果的标准差，不能写成置信区间；密度图和分布轮廓只用于显示已有记录。

## 九幅图的改法

| 正文编号 / 图文件 | 原图 | 新图 | 数据依据 |
| --- | --- | --- | --- |
| 图 2 / 图8_问题一求解流程 | 蓝灰双流程框图 | 紫、绿、橙卡片流程图，保留质量评价和配比建模的分支关系 | 已批准的流程图节点与连接 |
| 图 3 / 图0_A1-A3全量质量评分 | 水平柱状图 | 排序点图及均值±标准差，标记样本数 | 质量域评分_全量.csv |
| 图 4 / 图6_冲突与赋权诊断 | 两组柱状图，标签拥挤 | 前 12 对冲突的点图＋22 项熵权的水平点线图，以 M01—M22 对照指标 | 冲突指标对_top20.csv、质量指标熵权.csv |
| 图 5 / 图1_训练配比结构 | 堆叠柱图＋平均配比柱图 | 前 8 组配方的领域热图＋同顺序的平均占比点图 | A4 原始训练配比；推荐配比调整.csv 参考列 |
| 图 6 / 图2_线性模型R2 | R²、Pearson r 混列的柱图 | 左侧配对点图比较训练与同尺度检验 R²；右侧比较 60M、1B 的 Pearson r | 问题一_拟合汇总.csv |
| 图 7 / 图4_混合系数热力图 | 红蓝热图 | 有符号气泡矩阵；颜色标记符号，面积表示绝对值 | 混合系数矩阵_中心化.csv |
| 图 8 / 图3_预测vs实测 | 重叠散点 | 六边形分箱密度图及边缘分布，保留等值参考线 | 既有混合系数、截距和 1M 检验附件 |
| 图 9 / 图5_领域质量评分 | 代理得分柱图＋连续得分散点 | 代理得分排序点图＋六个映射领域的两套名次对照 | Loss代理领域难度.csv、质量与损失难度一致性.csv |
| 图 10 / 图7_外推与跨尺度 | 10B 箱线图＋60M/1M 柱图 | 10B 记录分布与四分位线＋60M/1M 均值±标准差点图 | 原始外推记录、外推尺度因子.csv、跨尺度损失比_60m_vs_1m.csv |

## 实施和核验计划

- [x] 新建独立数据读取模块 `求解/问题一/绘图数据.py`，按 index 和领域名匹配附件，读取现有参数；不导入会执行整套求解的 `问题一.py`。
- [x] 新建 `求解/问题一/重绘图表.py`，按上表生成 9 组 PNG、SVG，并写出绘图数据核验清单。
- [x] 计算绘图所需的既有参数预测值，核对 13 域 R² 与保存结果一致；核对中心化系数、配方均值、外推均值、跨尺度均值及标准差。
- [x] 逐张查看图片，检查完整性、字号、轴标签、图例和数值编码；调整拥挤位置。
- [x] 同步问题一图注、引用说明和图宽；保留全部公式、数据表及原有结果文件。
- [x] 检查改动范围和输入文件哈希。Q 分支提交使用 `[skip ci]` 跳过自动论文编译。

## 数据口径

用户提供的原绘图程序位于 `E:/华为杯f/研赛（华为杯）最终版(1)/研赛（华为杯）最终版/求解/问题一/问题一.py`，其绘图段与仓库现有程序一致。重绘以 Git 中论文引用的结果为准，明确读取 `质量指标熵权.csv`，不自动改用数值不同的 `_new.csv`。数据根目录通过 `--data-dir` 指定；默认仅搜索项目附近目录。

部分论文结论与实现的实质疑点仍按《问题一_修订决策与记录.md》单列。本轮图形变化不构成对这些结论的再次验证。

## 复现方法

在仓库根目录运行：

```powershell
python "求解/问题一/重绘图表.py" --data-dir "E:/华为杯f/real_attachments"
```

依赖 NumPy、Pandas、SciPy 和 Matplotlib 3.10 或更新版本，并安装宋体与 Times New Roman。此次运行使用 NumPy 2.2.6、Pandas 3.0.2、Matplotlib 3.10.8。程序输出 400 dpi PNG 和文字转路径的 SVG；LaTeX 直接引用 PNG，SVG 留作矢量排版。流程图的 LaTeX 入口仍为 `论文/figures/q1-workflow.tex`，绘图内容统一由 Python 维护。

可用 `--only 4 8` 仅重绘系数矩阵和流程图，或用 `--output-dir` 指定预览目录。核验清单记录本次实际重绘的图文件；发布完整九图时运行不带 `--only` 的命令。

## 已完成的核验

- 9 幅图已逐张检查并导出 PNG、SVG；正文的 9 个图片入口全部存在。
- 11 项数值核对通过。包括训练与 1M 检验的 13 域 R²、中心化系数、参考配比、训练平均损失、熵权之和、10B/70B 外推均值、60M/1M 均值和标准差，以及六领域 Spearman 系数。最大绝对差为浮点舍入量级。
- 19 个输入文件在绘图前后 SHA-256 相同，详见 `求解/问题一/图片/绘图数据核验.json`。预测图包含 3,328 个数据点，系数图展示 221 个矩阵项。
- 与 `8ca02a9` 比较，问题一 13 个公式环境、6 张完整数据表及全部引用标签未变；求解程序、结果文件未变。同步团队更新后，问题二至四与主分支 `335672a` 完全一致。
- 两个新增 Python 模块语法检查通过；`git diff --check` 通过。
- 整篇 PDF 未编译，其 SHA-256 仍为 `D46F30D9421263A2545812D846B55AAB7F23E64EEA21611BE494D43B1DBA0D52`。因此本次已核验独立图片及源文件引用，尚未核验新图在整篇 PDF 中的分页和浮动位置。

## 图文核对中保留待讨论的事项

按照用户“先只润色，实质问题稍后讨论”的决定，本次没有改写模型结论。图片按原结果如实显示，以下正文解释仍需下一轮处理：

1. 全量结果中 arxiv 均值约为 0.701，原正文排序段写作 0.703；这是既有图文数值差异，不是重绘改变结果。
2. 六领域名次对照中，github 的文本质量名次为第 6、损失代理名次为第 3；book 两套名次均为第 4。原文对“github 文本质量高”和“book 存在明显分歧”的解释需要按具体比较范围重新核对。Spearman 相关系数仍为 0.60。
3. 中心化系数的正负表示相对于同一验证域系数均值的位置，不能单凭气泡颜色解释为单独增加某领域的因果效果。原正文的领域作用解释仍按既有记录待讨论。

九图集中预览见 [问题一图片预览](问题一_图片预览.md)。
