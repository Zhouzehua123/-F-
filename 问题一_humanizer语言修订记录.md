# 问题一语言修订记录

日期：2026-09-26。工作分支：`humanizer`。基线：`origin/main` 的 `7aaa9a7e64c39046a4495ca3a51b5f39898c8e46`。

## 1. 修订范围与依据

本轮依据用户确认，使用以下技能对 `论文/5.1.问题一.tex` 进行语言润色：

- [academic-humanizer-zh](https://github.com/yllscar728/academic-humanizer-zh)：减少机械表达，保留事实、术语及论证关系。
- [latex-thesis-zh](https://github.com/bahayonghang/academic-writing-skills/tree/main/academic-writing-skills/latex-thesis-zh)：按自然段规划、改写与核对，保护数学环境和 LaTeX 引用。

两项技能已安装至本机 Codex 的 skills 目录。本轮直接读取规则及配套脚本执行。学位论文专用的章节和学校模板要求不适用于本项目，未据此改动竞赛论文结构。

语言参考为用户提供的 `E25102860076.pdf`，题名《机理约束下的高速列车轴承迁移学习诊断模型》。参考范围为 PDF 物理第 9–23 页，另实际查看第 18、22 页。仅借鉴论述方式，不复制其语句、方法、数据、图表或结论，也未将附件全文加入仓库。

采用的表达原则：

1. 交代具体数据差异，再说明方法选择。
2. 按已有计算顺序描述输入、处理与输出。
3. 公式前说明目的，公式后解释关键量。
4. 使用“计算、比较、估计、检验”等具体动词。
5. 结果分析紧接图表和已有数值，保留比较条件与适用范围。

参考文中的强断言、笔误和重复表达不作为模仿对象。润色质量通过语义与可读性审查，不用词频分数代替判断，也不将脚本结果解释为 AIGC 检测通过率。

## 2. 本轮修改

共修改 31 个自然段；原章节、段落顺序、表格字段及图表说明结构保持不变。完整润色稿见 `论文/5.1.问题一.tex`，逐句差异可在本分支与基线的 Git diff 中查看。

### 修改说明

- **删除无信息表达：** 压缩“这里能够计算代理值的是”“冲突比例还需要一个比较标准”等迂回铺垫；信息与限制条件仍保留。
- **调整句子结构：** 将口语化的操作说明改为明确的研究动作；梳理公式引入、增量检验和结果比较的句间关系。
- **待补证内容：** 未新增事实或论据。此前模型与实现的待讨论事项仍按《问题一_修订决策与记录.md》处理，语言修订不代表已完成实质审查。

| 源码行 | 修改重点 |
| --- | --- |
| 11 | 先说明指标差异，再介绍评分、冲突分析及配比回归 |
| 13 | 按输入、处理、检验和输出叙述两部分流程 |
| 27 | 明确配方与损失的对应关系及间接影响域的含义 |
| 37 | 将归一化与列表处理改为连续、正式的操作说明 |
| 84 | 区分熵权来源与指标重要程度 |
| 86 | 简化中位数填补说明，保留高低分偏置的限制 |
| 113 | 明确各候选方法分别处理哪类问题 |
| 137 | 结合相关系数解释赋权方法的选择 |
| 139 | 用占比约束引出系数稳定性与岭惩罚 |
| 146 | 压缩熵权公式前的计算说明 |
| 151 | 引出损失难度代理，保留平均配方条件 |
| 155 | 区分两套领域分类及评分含义 |
| 160 | 明确标准化得分差的比较对象 |
| 169 | 保留独立标准正态条件，引出冲突参照及排序一致性 |
| 179 | 将“排成”改为矩阵表示，明确行和约束 |
| 187 | 简化截距与配比系数无法唯一识别的说明 |
| 201 | 说明混合质量增量比较的目的与映射过程 |
| 205 | 保留增量小、共线性及后续分析之间的关系 |
| 221 | 简化数据分工与样本量说明 |
| 261 | 紧接差值解释两个可比较领域的结果 |
| 263 | 保留排序数值及其任务适用范围限制 |
| 296 | 合并三个统计集合的重复说法 |
| 298 | 区分统计显著性、排序一致程度和冲突比例 |
| 341 | 简化同尺度训练与检验表现的比较 |
| 343 | 将“损失基准变了”改为跨规模比较条件说明 |
| 347 | 保留质量项增量小与质量投入无效之间的区别 |
| 358 | 区分文本质量与领域拟合难度 |
| 364 | 明确无界结果、占比上限和有界方案数值 |
| 395 | 明确推荐的任务依据，保留“并不说明语料质量较低” |
| 397 | 保留外推估计与新增观测的区别 |
| 408 | 概括已有输出，保留指标选择与尺度调整的限制 |

以上行号对应本轮修订完成时的源文件；本轮改写未增删源码行。

## 3. 保护项与核对结果

各段均先生成单元清单，再使用 `polish_unit_zh.py --verify` 核对。31 段全部返回退出码 0 和 `PASS-SCRIPT`。最终另对整份 Q1 源文件进行一次核对，退出码为 0。

| 保护项 | 修订前后数量 | 核对方式与结果 |
| --- | --- | --- |
| 独立公式环境 | 13 | 按出现顺序比较源码，完全一致 |
| 行内数学片段 | 118 | 按出现顺序比较源码，完全一致 |
| 表格环境 | 6 | 连同列序、内容、表题逐字比较，完全一致 |
| 图片环境 | 9 | 图题、路径、标签及浮动参数逐字比较，完全一致 |
| 引文、交叉引用与标签 | 52 | 令牌及出现次数一致 |
| 数字令牌 | 507 | 数值字符串及出现次数一致 |
| LaTeX 命令 | 677 | 命令及出现次数一致 |

仅修改 Q1 正文及本记录。Q2–Q4、求解代码、数据和图像文件均未修改，也未重跑模型。

### 人工核对脚本候选

单元核对共有 16 条 B 类候选：14 条 `UP-NEG`、1 条 `UP-LENGTH`、1 条 `UP-STRENGTH`，没有 A 类阻断项。

| 行号 | 候选处理 |
| --- | --- |
| 27 | “没有对应验证列”改为“缺少对应验证列”，否定对象一致 |
| 37 | “不能”改为“无法”，仍限制列表值直接合成 |
| 84 | “不过”被计作否定词；指标重要程度的限制仍在 |
| 137 | “不容易直接说明”改为“难以直接解释”，含义一致 |
| 160 | “不同指标”换为“指标”，仍指此前两个指标的差值比较 |
| 187 | “不能唯一分开”改为“无法唯一识别”，限定对象一致 |
| 205 | “没有明显补充”改为“未形成明显补充”，增量结论一致 |
| 296 | “无论……还是……”被压缩，三个统计集合及“不明显改变”的判断均保留；可见汉字减少 21.3% |
| 298 | 加入“表明”衔接原有的“一致程度较弱”，未提高结论强度 |
| 341 | “没有出现”改为“未出现”，仍限定于当前对照 |
| 347 | “没有作用”改为“无效”，前面的“不能据此判断”保留 |
| 358 | 以“不必然对应”表达两种概念不等同，保留分别使用两类评分的结论 |
| 364 | “没有上限”改为“未设置上限”，“未舍入值”保留 |
| 395 | 独立复核发现初稿“而非”可能改变否定对象，已改为“但这并不说明 arxiv 语料质量较低”，并重新核对通过 |
| 408 | “跨规模损失所需的尺度调整”保留了不能直接沿用原尺度的限制 |

其他诊断中的提示也按语境判断：

- `E-ABSOLUTE` 4 条涉及“绝对值”“绝对损失”“中位绝对偏差”及“不必然”，不构成绝对化结论。
- `E-INCOMP` 1 条为承接前句“本问”的省略主语，原句语义完整。
- `E-NUMSPACE` 9 条涉及 1B、10B、70B 等规模标记，保留全文章节一致写法。
- `E-UNITFONT` 10 条将变量及范数记号识别为物理单位，未据此修改数学表达。
- `E-PUNCT` 1 条命中 LaTeX 数学间距命令及公式标点，未修改公式。
- 去模板化脚本提示“因此”频次及破折号数量；前者对应原有推理关系，后者包含表格缺项符号。未为降低计数机械换词或改表。
- 主张前置检查没有候选项。

`PASS-SCRIPT` 只说明脚本覆盖的保护项未发生阻断性变化。语义另经逐段独立复核；此前实质问题仍待用户决定。

```latex
% POLISH (论文/5.1.问题一.tex: 上表所列 31 段) [Severity: Info] [Priority: P3] [LLM]: 语言修订
% Changed: 口语化操作说明、迂回铺垫及方法与结果之间的句间衔接
% Protected: 方法步骤、样本与领域范围、数据、公式、图表、术语、引用及结论限制
% Meaning-Check: PRESERVED
% Risk-Flags: none
```

### 核对命令

本机用 `uv run --python C:/Users/admin/miniconda3/python.exe python` 执行已安装技能脚本。脚本目录为 `C:/Users/admin/.codex/skills/latex-thesis-zh/scripts`，下例中的 `$skill`、`$q1`、`$before`、`$after` 分别指该目录、Q1 源文件及各段修订前后快照。

```text
uv run --python C:/Users/admin/miniconda3/python.exe python $skill/polish_unit_zh.py $q1 --plan --json
uv run --python C:/Users/admin/miniconda3/python.exe python $skill/polish_unit_zh.py $q1 --verify --original $before --revised $after --max-growth 0.20 --json
uv run --python C:/Users/admin/miniconda3/python.exe python $skill/check_style_zh.py $q1 --goal clarity --strength moderate --json
uv run --python C:/Users/admin/miniconda3/python.exe python $skill/check_claim_forward.py $q1 --json
uv run --python C:/Users/admin/miniconda3/python.exe python $skill/deai_check.py $q1 --analyze
```

全部退出码为 0。精确参数、逐段快照、JSON 输出及最终保护项比较保存在工作区的 `tmp/humanizer/`，没有作为论文正文或数据上传。

## 4. 编译与版面核验

使用技能的 `compile.py` 包装器，在 WSL Ubuntu 中调用 XeLaTeX 与 latexmk。显式关闭 shell escape；通过本地字体适配入口加载论文，不向仓库加入系统字体。

最终编译返回 0，生成 97 页 PDF。日志中未出现缺字、Overfull 或未定义引用。另有封面 PDF 版本和重复页锚点的输出提示，本轮未涉及封面与模板。

已查看物理第 8–21 页的整体排版，并用 PDF.js 单独渲染第 9、20 页：中文表题完整可见，推荐配比表 10 行位于同一页。原稿已有的页末小节标题与独立浮动图页布局本轮保留。

本地 PDF 交付名为 `华为杯F题_humanizer_问题一语言修订.pdf`，位于工作区 `output/pdf/`。仓库中的旧 `论文.pdf` 未用本地临时编译产物覆盖。

## 5. 协作同步

开始本轮工作时已获取远端最新提交，在 `7aaa9a7` 上新建 `humanizer`。推送前再次获取远端、同步 `origin/main` 并核对改动范围；本轮仅提交 Q1 语言修改及本记录，不合入 `main`。最终远端版本与推送结果在交付消息中记录。

## 6. 后续同步与合并（2026-09-26）

第一轮已推送为 `e2d41625b6587aaa05e947a12b34bbf1d117119a`。随后用户明确要求拉取最新提交、解决冲突并将本分支合入 `main`，因此本节记录后续合并，不沿用上一阶段“暂不合入”的安排。

本次获取的主分支版本为 `3fd3627b49faee87c7e105ebb6fddfa795cc9e4a`。与 `humanizer` 合并时，只有 Q1 源文件出现冲突，共 3 个冲突区块，涉及 4 个自然段。处理方式如下：

- 领域均分：保留本分支的数值叙述，融合队友对“分类器信号加权”“可能随文档长度变化”及“限于本评分体系”的说明。
- Kendall 与冲突率：保留两版统计数值，明确统计显著性不能代替一致程度判断。
- 推荐配方：保留占比变化与加权平均损失目标，以及当前权重、约束条件下的解释；仍明确占比下降不说明文本质量较低。
- 跨尺度比例：保留全部均值、标准差，采用最新主分支关于“没有新增实测训练记录，仅检查估算比例稳定性”的限定。

主分支新增的 9 个精简图题、7 处 `[htbp]` 浮动参数和独立修改的分歧解释段落均保留；其余 27 处本分支独有的润色逐行核对保留。总体分析、问题二至四、模型检验及改进推广共 6 个文件与 `3fd3627` 完全一致。

合并后，13 个公式环境、118 个行内数学片段、6 张表格与两侧版本一致；52 个引用与标签、677 个 LaTeX 命令与最新主分支一致；9 个完整图片环境与最新主分支一致。当前数字令牌数为 489，与最新主分支相同；相较第一轮记录的 507，差异来自队友精简图题时去除的数字，正文计算结果未变。

本次未修改求解代码、数据或图片文件。冲突融合经过独立只读审查，并对合并后的完整论文重新编译；具体构建输出与保护项核对保存在工作区 `tmp/humanizer-merge/`。
