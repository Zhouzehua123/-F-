# 问题三、四流程图补充

## 分支与范围

- 分支：`expand`，起点为 `origin/main` 的 `44cab32`。
- 两个章节各增加一张流程图和一段引导文字。
- 图中步骤依据现有正文整理，问题三的初值数量另与求解代码核对。
- 不改变模型、数值结果、现有图表和问题一、二的内容。

## 图示内容

### 问题三

前两问的质量基线、配比修正和损失参数，与题设费用条件共同进入预算约束模型。采用多初值 SLSQP 求解，再分别进行三档预算比较、连续预算扫描、成本及上下文情景分析。

图中明确比较求解成功的结果；预算扫描失败点不补算，阶段边界按有效网格报告。图号由 LaTeX 自动生成，引用标签为 `fig:p3_workflow`。

### 问题四

按数据用途排列四条分析路径：损失与得分的联系、历史增量分解、条件前沿预测、任务瓶颈与宏观参照。保留 C1/C3 的不同整理口径、曲线区间与结构情景的区别，以及 C4 单独作宏观统计的边界。引用标签为 `fig:p4_workflow`。

## 图片与复现

- 源码：`求解/绘制问题三四流程图.py`。
- 运行：`python 求解/绘制问题三四流程图.py`，需要 Matplotlib、宋体和 Times New Roman。
- 输出：`论文/figures/q3-workflow.*` 和 `q4-workflow.*`。
- 正文引用嵌入字体的矢量 PDF；同时提供 SVG 与 400 dpi PNG。
- 沿用现有图表的紫、绿、蓝、金配色，中文宋体，英文 Times New Roman，公式使用 STIX 数学字体。
- `*-layout.json` 检查文字是否超出节点或画布，仍需结合论文页面检查图文布局。

## 核验

- 绘图程序不导入求解脚本，也不读取数据附件。
- 两张图的节点文字边界检查均通过。
- 本地编译使用临时字体配置读取系统字体，该配置不修改论文模板。
- XeLaTeX / latexmk 编译通过，共 97 页。流程图为图 19、图 23，分别位于 PDF 第 29、36 页，交叉引用正常，页面预览无裁切或文字重叠。
- 两张矢量图的字体全部嵌入。全文编译仍提示附录代码中的部分表情符号缺字，与此次新增图表无关。
